<<<<<<< HEAD
[general]
# Частота обновления кадров (установлено на 120 FPS)
framerate = 120

# Автоматическая регулировка чувствительности (1 = включено)
autosens = 0

# Настройки ширины и расстояния баров
# 0 означает отсутствие отступов — бары будут идти сплошной стеной
bar_width = 8
bar_spacing = 0

[input]
# Метод захвата звука. Автоматически выберет оптимальный (PipeWire/PulseAudio/ALSA)
# Если звука нет, раскомментируйте (уберите ;) нужный и впишите источник
; method = pulse
; source = auto

[output]
# Стандартный вывод в терминал через ncurses
method = ncurses

[color]
# Отключаем градиент (0 = выключен)
gradient = 0

# Глубокий багровый Abyss Crimson в формате HEX для сплошного цвета
# (Убедитесь, что ваш эмулятор терминала поддерживает TrueColor)
foreground = '#4A000A'

# Цвет фона (оставьте закомментированным, чтобы использовать прозрачность терминала)
; background = black

[smoothing]
# Сглаживание анимации (классический режим "Monstercat")
monstercat = 1
waves = 0
integral = 80
gravity = 120
=======
# Cava Audio Visualizer Configuration Template
# Optimized for smooth and responsive visualization

[general]
# Framerate (1-144) - higher = smoother but more CPU intensive
framerate = 120
bar_spacing = 2
bar_width = 8
[input]
# Audio input method: pulse, alsa, fifo, or portaudio
method = pulse
# Audio device (leave as default for auto-detection)
source = auto

[output]
# Output method: ncurses, terminal, raw, or circle
method = ncurses
# Terminal color scheme
style = stereo

[color]
# Color gradient for bars using template variables
gradient = 1
gradient_count = 2
gradient_color_1 = '#a80818'
gradient_color_2 = '#a80818'
#gradient_color_3 = '#d3d9ff'
#gradient_color_4 = '#bdc3ff'
#gradient_color_5 = '#b7b6ff'
#gradient_color_6 = '#ccc6ff'
#gradient_color_7 = '#c6b6ff'
#gradient_color_8 = '#ceb4eb'

[smoothing]
# Noise reduction (0-100) - higher = smoother but less responsive
# 77 is default, 85 provides good balance for smooth visualization
noise_reduction = 55

# Monstercat smoothing (0 or 1) - adds smoothing between adjacent bars
monstercat = 1

# Wave effect (0 or 1) - creates wave-like motion across bars
waves = 0

# Gravity (0-200) - controls how fast bars fall
# 100 = normal gravity, 150 = faster fall, 50 = slower fall
gravity = 120

[eq]
# Equalizer settings for frequency response
# Lower frequencies tend to be louder, so reduce them slightly
1 = 0.8
2 = 0.9
3 = 1.0
4 = 1.1
5 = 1.2
>>>>>>> 3050be824cfcb157c8266dd31709c48aa7df5959
